# WealthLog Frontend — copy to .env.local and fill in values

# ── API ───────────────────────────────────────────────────────────────────────
# Local dev: point to backend running locally or via Docker
NEXT_PUBLIC_API_URL=http://localhost:8001
