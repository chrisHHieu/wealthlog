"""Long-term and episodic memory for the agent.

Submodules:
- ``facts``: per-user fact CRUD + background extraction (long-term).
- ``episodic``: per-session summaries (episodic).
- ``prompts``: review/summary prompt templates.
"""
